{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for wish in wishes %}
            <li><a href="{{ path('wish_detail', {'id': wish.id}) }}">{{ wish.title }}</a></li>
        {% else %}
            <p>Aucun souhait en base de données.</p>
        {% endfor %}
    </ul>
{% endblock %}

{% block title %}

{% endblock %}

